import { LiftLogoDirective } from './lift-logo.directive';

describe('LiftLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new LiftLogoDirective();
    expect(directive).toBeTruthy();
  });
});
