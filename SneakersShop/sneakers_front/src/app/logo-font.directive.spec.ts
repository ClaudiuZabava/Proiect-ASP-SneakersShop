import { LogoFontDirective } from './logo-font.directive';

describe('LogoFontDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoFontDirective();
    expect(directive).toBeTruthy();
  });
});
